<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- Camera features -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" /> <!-- Queries for Android 11+ intent visibility -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT" />

            <data android:mimeType="image/*" />
        </intent>
    </queries>

    <application
        android:name=".ASLTranslatorApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/thlogonbg"
        android:roundIcon="@drawable/thlogonbg"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="false"
        android:theme="@style/Theme.ASLTranslator"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".activities.AchievementsActivity"
            android:exported="false" />
        <!-- FileProvider for camera image capture -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.asltranslator.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider> <!-- ✅ Splash Activity - LAUNCHER (Entry Point) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ASLTranslator.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- ✅ Authentication Activity - EXPORTED (can receive intents from Splash) -->
        <activity
            android:name=".activities.AuthActivity"
            android:exported="true"
            android:screenOrientation="portrait" /> <!-- ✅ Main Activity - EXPORTED (can receive intents from Auth/Splash) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait" /> <!-- Learning Activity -->
        <activity
            android:name=".activities.LearningActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ASLTranslator" /> <!-- Camera Translation Activity -->
        <activity
            android:name=".activities.CameraTranslateActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ASLTranslator" /> <!-- Regular Training Activity -->
        <activity
            android:name=".activities.TrainingActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ASLTranslator" /> <!-- 🔐 ADMIN ONLY: Admin Training Activity -->
        <activity
            android:name=".activities.AdminTrainingActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ASLTranslator">
            <meta-data
                android:name="admin_access_required"
                android:value="true" />
            <meta-data
                android:name="restricted_email"
                android:value="guylerner10@gmail.com" />
        </activity> <!-- 🎯 NEW: Practice Activity for Users -->
        <activity
            android:name=".activities.PracticeActivity"
            android:exported="false"
            android:parentActivityName=".activities.LearningActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ASLTranslator" /> <!-- Profile Activity -->
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ASLTranslator" />
    </application>

</manifest>